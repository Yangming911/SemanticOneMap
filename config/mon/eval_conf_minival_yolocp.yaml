EvalConf:
  multi_object: false
  max_steps: 1000
  max_dist: 1.0
  is_gibson: False
  object_nav_path: "datasets/objectnav_hm3d_v1/val_mini/content/"
  scene_path: "datasets/scene_datasets/"
  log_rerun: False
  use_pointnav: False
  square_im: True
  results_path: "results/minival_yolocp/"

config: [
  ./controller_habitat.yaml,
  ./mapping_conf_sim_yolocp.yaml,  # yolo=T + yolo-cp (GT calibration)
  ./planning_conf.yaml
]
